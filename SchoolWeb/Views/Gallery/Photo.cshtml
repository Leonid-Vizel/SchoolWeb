@using SchoolWeb.Models
@model PhotoModel
@{
    ViewData["Title"] = Model.Title;
}

<style>
    .center {
  display: block;
  margin-left: auto;
  margin-right: auto;
  width: 100%;
}
</style>

<div class = "text-center">
    <h1>@Model.Title</h1>
    <h3>@Model.Description</h3>
</div>
<div class = "container">
    <div class = "justify-content-center">
        <img src="@Model.ToString()" class="center">
    </div>
</div>